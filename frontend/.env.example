# Backend base URL for the Vite dev-server proxy and EventSource connections.
# Local dev: point to the Docker backend or a locally running backend.
# Netlify: override this with your deployed backend URL in
#   Netlify → Site configuration → Environment variables.
VITE_API_URL=http://localhost:3001
